{
  "name": "ziro",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run build --scope ziro && lerna publish"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "resolutions": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
